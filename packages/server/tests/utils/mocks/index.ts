/**
 * Mock Helpers - Central Export
 *
 * Exports all mock helper functions for easy importing across test files.
 * Usage: import { createMockLogger } from '@server/tests/utils/mocks';
 */

export { createMockLogger, type Logger } from './logger.mock';
